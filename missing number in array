   int missingNumber(vector<int>& arr) {
        int n= arr.size();
        int sum=0;
        int total= (n*(n+1))/2;
        for(int i=0;i<n;i++){
            sum = sum+arr[i];

        }
        int mn = total - sum;

      return mn;  
    }
