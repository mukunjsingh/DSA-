void moveZeroes(vector<int>& arr) {
        int n=arr.size();
        int temp=0;
        for(int i=0;i<n;i++){
            if(arr[i]!=0){
                arr[temp++]=arr[i];
            }
       
        }
        while(temp<n){
            arr[temp++]=0;
        } 
    }
